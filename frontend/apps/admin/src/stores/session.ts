import { computed, markRaw, ref } from 'vue';
import { defineStore } from 'pinia';
import { createIsmClient, type components } from '@ism/api-client';
const TOKEN_KEY='ism.admin.accessToken';
const ACTOR_KEY='ism.admin.actorId';
export const useSessionStore=defineStore('session',()=>{const accessToken=ref<string|undefined>(sessionStorage.getItem(TOKEN_KEY)??undefined);const actorId=ref<string|undefined>(sessionStorage.getItem(ACTOR_KEY)??undefined);const organizations=ref<components['schemas']['OrganizationNode'][]>([]);const currentOrganization=ref<components['schemas']['CurrentOrganization']>();const menus=ref<components['schemas']['MenuNode'][]>([]);const client=markRaw(createIsmClient({getAccessToken:()=>accessToken.value}));const organizationOptions=computed(()=>{const result:{id:string;label:string}[]=[];function walk(nodes:components['schemas']['OrganizationNode'][],depth=0){nodes.forEach(n=>{if(n.status==='ACTIVE')result.push({id:n.id,label:`${'—'.repeat(depth)}${n.name}`});walk(n.children,depth+1);});}walk(organizations.value);return result;});async function login(form:{tenantCode:string;username:string;password:string}){const {data,error}=await client.POST('/auth/login',{body:{...form,deviceId:crypto.randomUUID()}});if(error)throw new Error(error.error.message);accessToken.value=data.data.accessToken;actorId.value=data.data.user.id;sessionStorage.setItem(TOKEN_KEY,data.data.accessToken);sessionStorage.setItem(ACTOR_KEY,data.data.user.id);await loadWorkspace();}async function loadWorkspace(){if(!accessToken.value)return;const [tree,current,navigation]=await Promise.all([client.GET('/organizations/tree'),client.GET('/organizations/current'),client.GET('/navigation/menus')]);if(tree.error||current.error||navigation.error){logout();throw new Error('登录状态已失效');}organizations.value=tree.data.data;currentOrganization.value=current.data.data;menus.value=navigation.data.data;}async function switchOrganization(id:string){const {data,error}=await client.POST('/organizations/switch',{body:{organizationId:id}});if(error)throw new Error(error.error.message);currentOrganization.value=data.data;}function logout(){sessionStorage.removeItem(TOKEN_KEY);sessionStorage.removeItem(ACTOR_KEY);accessToken.value=undefined;actorId.value=undefined;organizations.value=[];menus.value=[];currentOrganization.value=undefined;}if(accessToken.value)void loadWorkspace().catch(()=>undefined);return{accessToken,actorId,organizations,currentOrganization,menus,organizationOptions,client,login,loadWorkspace,switchOrganization,logout};});
