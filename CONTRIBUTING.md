# 参与贡献

感谢参与项目。当前项目仍在基础建设阶段，请先通过Issue对齐需求和边界，再提交代码。

## 基本流程

1. 创建或认领Issue，明确功能编号、允许修改路径和验收编号。
2. 从最新`main`创建功能分支。
3. 同时提交实现、测试、迁移和必要文档。
4. 本地通过对应质量检查后提交Pull Request。

## 约束

- 不提交客户数据、内部地址、账号、密钥或未授权素材。
- 不跨模块引用Mapper、Entity或内部数据库表。
- 不绕过租户、权限、字段掩码、审计和幂等规则。
- 新增依赖必须说明必要性、许可证和替代方案。
- API或迁移变更必须同步更新契约与兼容说明。

完整开发环境和测试命令会在B0完成后补充。
